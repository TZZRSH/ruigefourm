spring:
  application:
    name: ruigeforum-gateway # 应用名称
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        enabled: true # 启用服务发现
        group: DEFAULT_GROUP # 所属组
        namespace: ruigeforum # 命名空间
        server-addr: 127.0.0.1:8888 # Nacos 服务器地址